# coding: utf-8
'''
describe:训练任模块
author:lxs
version:v2
date:2020class
'''


class ModelTrain(object):
    def __init__(self):
        pass
    def mid_model_init_train(self,data):
        # model_init_train(model_name,part_name,resolution,new_model_path,network_structure,optimizer,loss_value,callback,measure,dataset_path,iterate_times)
        return True
    def mid_model_ass_train(self,data):
        # index_calculation(model_file_path,dataset_path)
        return True
    def mid_model_opt_train(self,data):
        # model_opti_train(model_file_path,new_model_path,dataset_path)
        return True

model_train = ModelTrain()